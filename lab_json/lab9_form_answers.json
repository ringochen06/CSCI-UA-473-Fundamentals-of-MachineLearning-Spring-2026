{
  "lab9_s1": true,
  "lab9_code_step2": "import numpy as np\n\n# Fixed mini-batch (rows = items, cols = embedding dim)\ntext_z = text_z_fixed\nimage_z = image_z_fixed\ntemperature = 0.07\n\ndef symmetric_infonce(text_z, image_z, temperature):\n    # 1. L2 normalize\n    def normalize(x):\n        return x / (np.linalg.norm(x, axis=1, keepdims=True) + 1e-8)\n\n    text_z = normalize(text_z)\n    image_z = normalize(image_z)\n\n    # 2. logits = similarity matrix\n    logits = text_z @ image_z.T / temperature   # (N, N)\n\n    # 3. labels\n    N = logits.shape[0]\n    labels = np.arange(N)\n\n    # 4. cross entropy (manual)\n    def cross_entropy(logits, labels):\n        # stable softmax\n        logits = logits - np.max(logits, axis=1, keepdims=True)\n        exp = np.exp(logits)\n        probs = exp / np.sum(exp, axis=1, keepdims=True)\n\n        # pick correct class probs\n        correct = probs[np.arange(len(labels)), labels]\n\n        return -np.mean(np.log(correct + 1e-8))\n\n    loss_t2i = cross_entropy(logits, labels)\n    loss_i2t = cross_entropy(logits.T, labels)\n\n    loss = 0.5 * (loss_t2i + loss_i2t)\n\n    return loss\n\n\nloss = symmetric_infonce(text_z, image_z, temperature)\nprint(\"loss:\", float(loss))\n",
  "lab9_synthetic": false,
  "lab9_c2": "loss: 5.3455705642700195\n",
  "lab9_code_step3": "import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\ntorch.manual_seed(42)\n\ntext_dim = text_dim_ctx    # 768\nimage_dim = image_dim_ctx  # 384\nhidden_dim = hidden_dim_ctx  # 256\nshared_dim = shared_dim_ctx  # 128\ntrain_batch = train_batch_ctx\ntrain_lr = train_lr_ctx\n\nXtr_t = Xtr_t_ctx  # (n_train, 768)\nXtr_i = Xtr_i_ctx  # (n_train, 384)\nXva_t = Xva_t_ctx  # (n_val, 768)\nXva_i = Xva_i_ctx  # (n_val, 384)\n\n\nclass DualProj(nn.Module):\n    \"\"\"One hidden ReLU layer per modality, then shared_dim (L2-normalized).\"\"\"\n\n    def __init__(self, dt, di, dh, ds):\n        super().__init__()\n        self.mlp_t = nn.Sequential(\n            nn.Linear(dt, dh),\n            nn.ReLU(),\n            nn.Linear(dh, ds),\n        )\n        self.mlp_i = nn.Sequential(\n            nn.Linear(di, dh),\n            nn.ReLU(),\n            nn.Linear(dh, ds),\n        )\n\n    def forward(self, t, i):\n        zt = F.normalize(self.mlp_t(t), p=2, dim=1)\n        zi = F.normalize(self.mlp_i(i), p=2, dim=1)\n        return zt, zi\n\n\nmodel = DualProj(text_dim, image_dim, hidden_dim, shared_dim)\nopt = torch.optim.Adam(model.parameters(), lr=train_lr)\n\n\ndef symmetric_infonce_torch(zt, zi, temperature=0.07):\n    logits = zt @ zi.T / temperature\n    labels = torch.arange(logits.size(0), device=logits.device)\n    loss_t2i = F.cross_entropy(logits, labels)\n    loss_i2t = F.cross_entropy(logits.T, labels)\n    return 0.5 * (loss_t2i + loss_i2t)\n\n\n# training loop\nn_train = Xtr_t.size(0)\nepochs = 100\ntemperature = 0.07\n\nfor epoch in range(epochs):\n    model.train()\n    perm = torch.randperm(n_train)\n    epoch_loss = 0.0\n    num_batches = 0\n\n    for start in range(0, n_train, train_batch):\n        idx = perm[start:start + train_batch]\n        bt = Xtr_t[idx]\n        bi = Xtr_i[idx]\n\n        zt, zi = model(bt, bi)\n        loss = symmetric_infonce_torch(zt, zi, temperature)\n\n        opt.zero_grad()\n        loss.backward()\n        opt.step()\n\n        epoch_loss += loss.item()\n        num_batches += 1\n\n    final_train_loss = epoch_loss / num_batches\n\n\n# validation Recall@1\nmodel.eval()\nwith torch.no_grad():\n    zt_val, zi_val = model(Xva_t, Xva_i)   # both normalized already\n\n    sim = zt_val @ zi_val.T   # (n_val, n_val)\n\n    labels = torch.arange(sim.size(0), device=sim.device)\n\n    # text -> image\n    pred_i = sim.argmax(dim=1)\n    recall_t2i = (pred_i == labels).float().mean().item()\n\n    # image -> text\n    pred_t = sim.argmax(dim=0)\n    recall_i2t = (pred_t == labels).float().mean().item()\n\nprint(\"final_train_loss\", final_train_loss)\nprint(\"recall_t2i\", recall_t2i, \"recall_i2t\", recall_i2t)",
  "lab9_s4": true,
  "lab9_retrieval_model_version": 9,
  "lab9_c4": "mean_top1_per_tau [np.float32(0.9998238), np.float32(0.9887461), np.float32(0.8758757), np.float32(0.3688319), np.float32(0.14506495)]\n",
  "lab9_code_step4": "import numpy as np\n\n# Fixed normalized embeddings (N x d), unit rows\ntext_z = text_z_temp    # (N, d)\nimage_z = image_z_temp  # (N, d)\n\ntaus = [0.03, 0.07, 0.15, 0.4, 1.0]\n\n# For each tau:\n#   logits = text_z @ image_z.T / tau   → (N, N)\n#   softmax rows (subtract row max for stability)\n#   mean_top1 = mean of diagonal probabilities  (confidence on correct match)\n# TODO: fill mean_top1_per_tau in taus order\n\nmean_top1_per_tau = []\n\nfor tau in taus:\n    # 1. logits\n    logits = (text_z @ image_z.T) / tau   # (N, N)\n\n    # 2. row-wise softmax (stable)\n    logits = logits - np.max(logits, axis=1, keepdims=True)\n    exp = np.exp(logits)\n    probs = exp / np.sum(exp, axis=1, keepdims=True)\n\n    # 3. diagonal = correct match probability\n    diag_probs = np.diag(probs)\n\n    mean_top1 = np.mean(diag_probs)\n    mean_top1_per_tau.append(mean_top1)\n\nprint(\"mean_top1_per_tau\", mean_top1_per_tau)\n",
  "lab9_c1": "text_out shape (4, 32) row norm 0.99999994\nimage_out shape (4, 32)\n",
  "lab9_s2": true,
  "lab9_c3": "final_train_loss 0.046560225822031495\nrecall_t2i 0.0416666679084301 recall_i2t 0.0416666679084301\n",
  "lab9_code_step1": "import numpy as np\n\n# Linear maps (given): text 768 -> 32, image 384 -> 32\nW_text = W_text_fixed   # shape (768, 32)\nb_text = b_text_fixed   # shape (32,)\nW_image = W_image_fixed  # shape (384, 32)\nb_image = b_image_fixed  # shape (32,)\n\ntext_emb = text_emb_batch    # (N, 768)\nimage_emb = image_emb_batch  # (N, 384)\n\n\ndef project_text(x, W, b):\n    # linear projection\n    z = x @ W + b   # (N, 32)\n\n    # L2 normalize (row-wise)\n    norm = np.linalg.norm(z, axis=1, keepdims=True)  # (N, 1)\n    z = z / norm\n\n    return z\n\n\ndef project_image(x, W, b):\n    # linear projection\n    z = x @ W + b   # (N, 32)\n\n    # L2 normalize (row-wise)\n    norm = np.linalg.norm(z, axis=1, keepdims=True)\n    z = z / norm\n\n    return z\n\n\ntext_out = project_text(text_emb, W_text, b_text)\nimage_out = project_image(image_emb, W_image, b_image)\nprint(\"text_out shape\", text_out.shape, \"row norm\", np.linalg.norm(text_out[0]))\nprint(\"image_out shape\", image_out.shape)\n"
}
